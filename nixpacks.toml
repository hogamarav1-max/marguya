[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo_mysql", "php82Extensions.gd", "php82Extensions.zip", "php82Extensions.curl", "php82Extensions.mbstring"]

[phases.install]
cmds = ["echo 'PHP application - no build step required'"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t . router.php"
